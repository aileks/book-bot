# Book-Bot

Small CLI utility that gets length and letter counts of a text.

## Requirements

- Python version 3.11+
- Optional: `uv` version 0.9+

## Usage

```sh
python3 main.py books/my_book.txt

# or with uv
uv run main.py books/my_book.txt
```
